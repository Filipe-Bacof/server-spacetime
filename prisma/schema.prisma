generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// NPX PRISMA MIGRATE DEV
// NPX PRISMA STUDIO

model User {
  id   String @id @default(uuid())
  name String
}
